# GitHub Actions

This directory is reserved for future GitHub Actions workflows.

Recommended future workflows:

- Godot headless validation
- Android export
- Release asset packaging
- Tag-triggered release

Do not claim CI verification is complete until an actual workflow exists and passes.
